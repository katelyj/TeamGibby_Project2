from flask import Flask, render_template, request, session, redirect, url_for
from utils import add, create, login
import sqlite3
import hashlib

app = Flask(__name__)

@app.route("/")
def root():
    for user in session: 
        return redirect(url_for("main"))
    return redirect(url_for("auth"))

@app.route("/auth/")
def auth():
    checkL
    return render_template("login.html")
    
@app.route("/main/")


@app.route("/logout/", methods = ["POST"])
def logout():
    if request.form["enter"] == "logout":
        session.pop("user")
    return redirect(url_for("auth"))

@app.route("/create/", methods = ["GET", "POST"])
def create():
    
@app.route("/view/")
def view():

@app.route("/add/")
def add():

@app.route("/settings/")
def settings():


if __name__ == "__main__":
    app.debug = True
    app.run()
